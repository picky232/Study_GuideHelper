# 학습 설계 도우미 — 개발 계획서

> 기간: 2026/06/24 ~ 2026/07/07 (14일)  
> 형태: PWA (설치형 웹앱)  
> 핵심 엔진: Claude API

---

## 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | 학습 설계 도우미 |
| 목적 | 목표 입력 시 AI가 마감일까지 맞춤형 학습 계획 자동 설계 |
| 형태 | PWA · 설치형 웹앱 |
| 핵심 엔진 | Claude API |
| 배포 | Vercel |

---

## 기술 스택

| 항목 | 선택 | 비고 |
|------|------|------|
| 프론트엔드 | React (Vite) + PWA | 설치형 웹앱 |
| 백엔드 | Vercel Serverless Functions (`/api`) | Sleep 없음, 무료 |
| DB | Supabase (PostgreSQL) | 인증 내장, 무료 |
| AI | Claude API | 계획 생성·재조정·피드백 |
| 알림 | Vercel Cron + Firebase FCM | 정시 푸시 알림 |
| 배포 | Vercel | 프론트+백엔드+Cron 통합 |

---

## 시스템 아키텍처

```
[사용자 PWA (React)]
        ↕ REST API
[Vercel Serverless Functions]
    ├── /api/auth          → 회원가입 / 로그인 (JWT)
    ├── /api/generate      → Claude API 호출 → 계획 생성·재조정
    ├── /api/schedule      → 일정 CRUD
    ├── /api/feedback      → Claude API → 피드백 메시지
    └── /api/notify        → FCM 푸시 발송

[Vercel Cron Job] → 매일 지정 시간 /api/notify 실행
        ↓
[Firebase FCM] → 사용자 기기 푸시 알림

[Supabase PostgreSQL]
    ├── users       (사용자 정보)
    ├── goals       (목표 정보)
    ├── schedules   (일자별 학습 태스크)
    └── reviews     (복습 사이클 일정)
```

---

## 핵심 기능 정의

| 기능 | 설명 |
|------|------|
| F1 목표 설정 | 과목·시험일·하루 공부시간·학습 범위·현재 수준 입력 |
| F2 AI 계획 생성·재조정 | Claude API → 일자별 태스크 자동 분배, 미완료 시 잔여 일정 재조정 |
| F3 캘린더·완료 체크 | 월간/주간 캘린더, 완료 체크, 달성률·진행률 시각화 |
| F4 푸시 알림 | FCM 토큰 등록, Cron 정시 리마인더, 미수행 시 재알림 |
| F5 학습 피드백 | 달성률 기반 AI 코칭 메시지, 주간 리포트, 계획 재조정 제안 |
| F6 복습 사이클 | 망각 곡선(1·3·7·14일) 기반 복습 태스크 자동 삽입 |

---

## 화면 구성

```
홈 (대시보드)
├── 오늘 날짜·태스크 요약 카드
├── 달성률 원형 차트
├── 오늘 태스크 체크리스트
└── AI 코칭 메시지 (F5)

목표 입력 (F1)
├── STEP 1: 과목·시험 종류 선택
├── STEP 2: 마감일·하루 공부시간 설정
├── STEP 3: 학습 범위·현재 수준 입력
└── AI로 계획 생성 버튼 → 미리보기 → 확정

캘린더 (F3·F6)
├── 월간 / 주간 뷰 전환
├── 날짜별 태스크 표시 (학습·복습 구분)
├── 날짜 클릭 → 상세 태스크 팝업
└── 이번 주 달성률 진행바

피드백 (F5)
├── 주간 학습 리포트
├── 달성률 추이 차트
└── AI 계획 재조정 제안

설정
├── 알림 시간 설정
├── 하루 공부 가능 시간대 설정
└── 계정·데이터 관리
```

---

## 날짜별 세부 개발 계획

---

### 📅 6/24 (수) — 프로젝트 초기 세팅

**목표: 개발 환경 완성 + 첫 화면 렌더링**

- [x] Vite + React 프로젝트 생성
  - `npm create vite@latest study-planner -- --template react`
  - JS 적용 (속도 우선, plan 권장대로)
- [x] 폴더 구조 설계
  - plan.md 평면구조 대신 DDD 4계층 구조 채택 (`FrontEnd/claude.md` 참고: domains/infrastructure/presentation/app)
- [x] 필수 라이브러리 설치
  - `react-router-dom` (라우팅)
  - `@supabase/supabase-js` (DB 연동)
  - `axios` (API 호출)
  - `react-calendar` (캘린더)
  - `tailwindcss` + `@tailwindcss/vite` (스타일링)
- [x] Vercel 프로젝트 생성 + GitHub 연결 (자동 배포 설정) — dev/main 브랜치 분리, PR 머지 확인됨
- [ ] `.env` 파일 구성 (Supabase URL·Key, Claude API Key, FCM Key)
  - FrontEnd `.env`: Firebase 값 입력 완료
  - BackEnd `.env`: Supabase URL·Service Role Key 입력 완료 (6/25). Claude/FCM 키는 해당 작업일에 진행
- [x] 기본 라우팅 설정 (`/`, `/goal/new`, `/calendar`, `/feedback`, `/settings`, `/login`, `/signup`)

**완료 기준:** `npm run dev` 실행 시 기본 레이아웃 렌더링 확인

---

### 📅 6/25 (목) — Supabase 인증 + DB 스키마

**목표: 회원가입·로그인 동작 + DB 테이블 완성**

- [x] Supabase 프로젝트 생성
- [x] DB 테이블 설계 및 생성
  ```sql
  -- 사용자
  users (id, email, name, daily_study_hours, notify_time, created_at)

  -- 목표
  goals (id, user_id, subject, exam_type, deadline, daily_hours,
         study_range, current_level, status, created_at)

  -- 일자별 학습 태스크
  schedules (id, goal_id, user_id, date, title, duration_min,
             is_done, is_review, review_round, created_at)

  -- FCM 토큰
  fcm_tokens (id, user_id, token, created_at)
  ```
- [x] Row Level Security (RLS) 설정 — 본인 데이터만 접근 가능 (4개 테이블 정책 적용 완료)
- [x] 회원가입 페이지 UI 구현
  - 이메일·비밀번호·이름 입력
  - Supabase Auth 연동 (`api/auth/signup` 경유, DB row 생성 확인)
- [x] 로그인 페이지 UI 구현
  - 이메일·비밀번호 로그인
  - JWT 토큰 저장 (localStorage) — accessToken 발급 확인
- [x] 로그인 상태 전역 관리 (Context API — `AuthContext`)
- [x] 보호 라우팅 설정 (`ProtectedRoute`, 비로그인 시 `/login` 리다이렉트)

**완료 기준:** 회원가입 → 로그인 → 홈 이동 동작 확인 — `vercel dev` + curl로 signup/login API 전체 흐름 검증 완료 (6/25)

---

### 📅 6/26 (금) — 홈 대시보드 UI

**목표: 홈 화면 완성 (데이터 연동 전 목업 데이터로)**

- [ ] 홈 레이아웃 구성
  - 상단: 오늘 날짜·요일 표시
  - 중단: 달성률 원형 차트 (오늘 완료/전체 태스크)
  - 하단: 오늘 태스크 체크리스트
- [ ] 오늘 태스크 카드 컴포넌트
  - 태스크명, 소요 시간, 완료 체크박스
  - 복습 태스크는 별도 배지 표시
- [ ] 하단 네비게이션 바 (홈 / 캘린더 / 피드백)
- [ ] AI 코칭 메시지 영역 (달성률 기반 텍스트 표시)
- [ ] 목업 데이터로 전체 UI 확인

**완료 기준:** 홈 화면 UI 완성 (정보처리기사 예시 데이터 기준)

---

### 📅 6/27 (토) — 목표 입력 UI (F1)

**목표: 목표 입력 폼 완성 + Supabase 저장**

- [ ] 목표 입력 STEP 1 — 과목·시험 종류
  - 과목명 텍스트 입력
  - 시험 종류 선택 (자격증 / 내신 / 수능 / 직접 입력)
- [ ] 목표 입력 STEP 2 — 마감일·공부시간
  - 날짜 피커 (시험일 선택)
  - 하루 공부시간 선택 버튼 (1h / 2h / 3h / 4h / 직접 입력)
  - D-day 자동 계산 표시
- [ ] 목표 입력 STEP 3 — 학습 범위·현재 수준
  - 학습 범위 텍스트 입력 (예: "1~5과목 전체")
  - 현재 수준 슬라이더 (0~100%)
- [ ] 입력값 유효성 검사 (마감일 과거 선택 방지 등)
- [ ] Supabase `goals` 테이블에 저장
- [ ] 저장 완료 후 AI 계획 생성 페이지로 이동

**완료 기준:** 목표 입력 → Supabase 저장 → 다음 단계 이동 확인

---

### 📅 6/28 (일) — Claude API 연동 + 계획 생성 (F2)

**목표: AI 계획 생성 API 완성**

- [ ] Vercel Serverless Function 생성 (`/api/generate.js`)
  ```
  입력: 과목, 마감일, 하루 공부시간, 학습 범위, 현재 수준
  처리: Claude API 호출
  출력: 일자별 태스크 배열 (JSON)
  ```
- [ ] Claude API 프롬프트 설계
  ```
  - 역할: 학습 설계 전문가
  - 입력 정보 파싱 → 총 학습 분량 산정
  - 하루 공부시간 기준으로 일자별 분배
  - 망각 곡선 복습 일정 포함
  - JSON 형식으로 반환 (date, title, duration_min, is_review)
  ```
- [ ] 계획 생성 로딩 화면 UI (스피너 + "AI가 계획을 설계 중입니다...")
- [ ] 생성된 계획 미리보기 화면
  - 날짜별 태스크 목록
  - 수정 가능 (태스크 삭제·추가·순서 변경)
- [ ] 확정 버튼 → Supabase `schedules` 테이블에 일괄 저장

**완료 기준:** 목표 입력 → AI 계획 생성 → 미리보기 → DB 저장 전체 흐름 확인

---

### 📅 6/29 (월) — 계획 재조정 + 홈 데이터 연동 (F2)

**목표: 실데이터 연동 + 미완료 시 재조정 로직**

- [ ] 홈 화면에 실데이터 연동
  - 오늘 날짜 기준으로 `schedules` 테이블 조회
  - 달성률 계산 (완료 태스크 / 전체 태스크)
- [ ] 완료 체크 기능 구현
  - 체크박스 클릭 → `is_done` 업데이트
  - 달성률 실시간 갱신
- [ ] 미완료 재조정 API (`/api/regenerate.js`)
  - 미완료 태스크 감지
  - 잔여 일정 기준으로 Claude API 재호출
  - 기존 미완료 이후 일정 덮어쓰기
- [ ] 재조정 트리거 조건 설정
  - 하루 달성률 50% 미만 → 다음날 홈 접속 시 재조정 제안 팝업

**완료 기준:** 완료 체크 → 달성률 갱신 → 미완료 재조정 동작 확인

---

### 📅 6/30 (화) — 캘린더 뷰 구현 (F3)

**목표: 월간·주간 캘린더 완성**

- [ ] 캘린더 라이브러리 세팅 (FullCalendar or react-calendar)
- [ ] 월간 뷰
  - 날짜별 태스크 수 표시 (점 또는 숫자 배지)
  - 완료 날짜 색상 구분 (완료 / 미완료 / 오늘)
  - 날짜 클릭 → 상세 팝업
- [ ] 주간 뷰
  - 시간대별 태스크 블록 표시
  - 이번 주 달성률 진행바
- [ ] 날짜 상세 팝업
  - 해당 날짜 전체 태스크 목록
  - 완료 체크 가능
  - 복습 태스크 별도 표시 (배지)
- [ ] 월간 ↔ 주간 뷰 전환 버튼

**완료 기준:** 캘린더에 실데이터 표시 + 완료 체크 동작 확인

---

### 📅 7/1 (수) — 복습 사이클 구현 (F6)

**목표: 망각 곡선 기반 복습 태스크 자동 삽입**

- [ ] 복습 사이클 로직 구현
  ```
  학습 완료 → 1일 후 복습 1회차 삽입
             → 3일 후 복습 2회차 삽입
             → 7일 후 복습 3회차 삽입
             → 14일 후 복습 4회차 삽입
  ```
- [ ] 계획 생성 시 복습 태스크 동시 생성 (이미 F2에서 Claude 프롬프트에 포함)
- [ ] 복습 태스크 UI 구분
  - 캘린더에서 복습 태스크 별도 색상 표시
  - 홈 체크리스트에서 "복습" 배지 표시
- [ ] 복습 완료 시 다음 복습 사이클 자동 등록
- [ ] 마감일 초과 복습 태스크 처리 (마감일 내로 압축 or 제외)

**완료 기준:** 학습 완료 후 복습 태스크가 캘린더에 자동 삽입 확인

---

### 📅 7/2 (목) — FCM 푸시 알림 세팅 (F4)

**목표: Firebase 설정 + FCM 토큰 등록**

- [ ] Firebase 프로젝트 생성 + FCM 설정
- [ ] 프론트엔드 FCM 연동
  - `firebase` 라이브러리 설치
  - Service Worker 등록 (`firebase-messaging-sw.js`)
  - 알림 권한 요청
  - FCM 디바이스 토큰 발급
  - 토큰 → Supabase `fcm_tokens` 테이블 저장
- [ ] 알림 수신 핸들러 구현
  - 앱 포그라운드: 인앱 토스트 알림
  - 앱 백그라운드: 시스템 푸시 알림
- [ ] `/api/notify.js` Serverless Function 구현
  - Supabase에서 오늘 학습 예정 사용자 목록 조회
  - FCM Admin SDK로 각 사용자에게 푸시 발송
  - 발송 내용: "오늘 [과목] 학습 시작할 시간입니다! 📚"

**완료 기준:** 버튼 클릭으로 수동 FCM 푸시 발송 + 기기 수신 확인

---

### 📅 7/3 (금) — Vercel Cron + 알림 자동화 (F4)

**목표: 정시 자동 알림 완성**

- [ ] `vercel.json` Cron Job 설정
  ```json
  {
    "crons": [
      {
        "path": "/api/notify",
        "schedule": "0 * * * *"
      }
    ]
  }
  ```
  - 매 정시마다 실행 → 해당 시간 알림 설정 사용자에게 발송
- [ ] 사용자별 알림 시간 설정 기능 (설정 페이지)
  - 알림 시간 선택 (시:분 피커)
  - Supabase `users.notify_time` 업데이트
- [ ] 미수행 재알림 로직
  - 당일 23:00 Cron → 미완료 사용자에게 "아직 학습을 완료하지 않았어요!" 발송
- [ ] Cron 실행 로그 확인 (Vercel 대시보드)

**완료 기준:** 지정 시간에 자동 푸시 알림 수신 확인

---

### 📅 7/4 (토) — AI 학습 피드백 (F5)

**목표: 피드백 페이지 + AI 코칭 메시지 완성**

- [ ] `/api/feedback.js` Serverless Function 구현
  - 사용자 주간 달성률 집계
  - Claude API 호출 → 코칭 메시지 생성
  - 프롬프트: "달성률 [X]%, 미완료 과목 [Y] 기반으로 격려·조언 메시지 작성"
- [ ] 홈 화면 AI 코칭 메시지 연동
  - 매일 홈 접속 시 달성률 기반 메시지 표시
  - 달성률 구간별 톤 변화
    - 80% 이상: 격려
    - 50~80%: 조언
    - 50% 미만: 재조정 제안
- [ ] 피드백 페이지 구현
  - 주간 학습 리포트 (요일별 달성률 바 차트)
  - 과목별 진행률
  - AI 주간 피드백 메시지
  - 계획 재조정 제안 버튼

**완료 기준:** 피드백 페이지 UI + AI 메시지 동작 확인

---

### 📅 7/5 (일) — PWA 설정 + 통합 테스트 1

**목표: PWA 설치 가능 상태 + 전체 흐름 1차 테스트**

- [ ] PWA 설정
  - `manifest.json` 작성 (앱 이름·아이콘·테마 컬러)
  - Service Worker 등록 (`vite-plugin-pwa` 사용)
  - 오프라인 캐싱 전략 설정 (홈·캘린더 페이지)
  - 홈 화면 추가 설치 배너 동작 확인
- [ ] 전체 서비스 흐름 1차 테스트
  - 회원가입 → 로그인 → 목표 입력 → AI 계획 생성 → 홈 확인
  - 완료 체크 → 달성률 갱신
  - 캘린더 확인
  - 알림 수신

**완료 기준:** 모바일 기기에서 PWA 설치 + 전체 흐름 동작 확인

---

### 📅 7/6 (월) — 버그 수정 + UI 다듬기

**목표: 발견된 버그 수정 + 사용성 개선**

- [ ] 1차 테스트에서 발견된 버그 목록 작성
- [ ] 우선순위 버그 수정 (기능 동작 관련)
- [ ] UI 개선 사항 적용
  - 로딩 상태 처리 (스피너, 스켈레톤)
  - 에러 메시지 UI (API 실패, 네트워크 오류)
  - 빈 상태 UI (목표 없을 때, 오늘 태스크 없을 때)
- [ ] 모바일 반응형 최종 확인 (iPhone·Android)
- [ ] Claude API 에러 핸들링 (타임아웃, 응답 파싱 실패)

**완료 기준:** 주요 버그 수정 + 모바일 UI 정상 동작 확인

---

### 📅 7/7 (화) — 최종 테스트 + 배포 마무리

**목표: 배포 URL 완성 + 시연 빌드 확정**

- [ ] 2차 통합 테스트 (처음부터 끝까지 전체 시나리오)
  - 시나리오 1: 정보처리기사 60일 준비 플로우
  - 시나리오 2: 기말고사 2주 준비 플로우
- [ ] Vercel 환경변수 최종 확인 (API Key 누락 여부)
- [ ] Vercel Cron 동작 확인 (로그 확인)
- [ ] Supabase RLS 보안 최종 점검
- [ ] README.md 작성
  - 프로젝트 소개
  - 기술 스택
  - 실행 방법
  - 배포 URL
- [ ] 시연 시나리오 정리 (발표용)
- [ ] 최종 배포 URL 확인

**완료 기준:** 배포 URL 접속 + 전체 기능 정상 동작 확인

---

## 예상 결과물

| 결과물 | 내용 |
|--------|------|
| 설치형 PWA 앱 | 홈 화면 설치, 오프라인 접근, F1~F6 전체 동작 |
| AI 학습 계획 엔진 | 목표 입력 → 일자별 계획 자동 생성, 미완료 시 재조정 |
| 알림·체크 루프 | Cron+FCM 푸시 → 완료 체크 → 달성률 → 복습 사이클 |
| 배포 빌드 | Vercel 배포 URL + 시연용 시나리오 |

---

## 비용 구조

| 서비스 | 플랜 | 비용 |
|--------|------|------|
| Vercel | 무료 | $0 |
| Supabase | 무료 | $0 |
| Firebase FCM | 무료 | $0 |
| Claude API | 사용량 기반 | 소량 과금 |
| **합계** | | **거의 $0** |

---

## 핵심 지표 (완료 기준)

| 지표 | 목표 |
|------|------|
| 계획 생성 완료율 | 목표 입력 후 AI 계획 생성 성공률 95% 이상 |
| 일일 체크율 | 사용자 테스트 기준 태스크 완료 체크 동작 100% |
| 알림 수신율 | Cron 실행 후 FCM 발송 성공률 95% 이상 |
| PWA 설치 | iOS·Android 홈 화면 설치 정상 동작 |